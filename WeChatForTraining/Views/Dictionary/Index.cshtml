@model IEnumerable<WeChatForTraining.Models.Dic_Subject>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/managerLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sub_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sub_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sub_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sub_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sub_id })
        </td>
    </tr>
}

</table>
